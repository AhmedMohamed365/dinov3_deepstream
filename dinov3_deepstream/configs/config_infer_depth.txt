[property]
gie-unique-id=2

# depth head
onnx-file=/dinov3_deepstream/dinov3_models/head_depth/model.onnx
model-engine-file=/dinov3_deepstream/dinov3_models/head_depth/model.onnx_b1_gpu0_fp16.engine

gpu-id=0
batch-size=1
network-mode=2          # FP16
process-mode=1          # full-frame (still OK; input is tensor-from-meta)
network-type=100        # "other"
output-tensor-meta=1

# IMPORTANT: consume tensor attached on buffer as GstNvDsPreProcessBatchMeta
input-tensor-from-meta=1